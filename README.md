# 🚀 Автоматизация публикаций в Яндекс.Дзен

## ✅ Что создано

### 1. Система конвертации дайджестов
- **`convert_digest_to_article.py`** — конвертирует Telegram дайджесты в статьи для Дзен
- **`generate_rss_simple.py`** — генерирует RSS ленту

### 2. Статический сайт
- **`public/index.html`** — лендинг с информацией о RSS
- **`public/rss.xml`** — RSS лента для Яндекс.Дзен
- **`public/articles/`** — статьи в JSON и Markdown

---

## 🎯 Архитектура

```
[Telegram дайджесты] 
    ↓
[convert_digest_to_article.py] → Конвертация в статью
    ↓
[public/articles/] → Сохранение JSON + MD
    ↓
[generate_rss_simple.py] → Генерация RSS
    ↓
[public/rss.xml] → RSS лента
    ↓
[Vercel Deploy] → Публикация
    ↓
[Яндекс.Дзен] → Автоматический импорт
```

---

## 🚀 Деплой на Vercel

### Вариант 1: Через Vercel CLI (быстрый)

```bash
# Установить Vercel CLI
npm i -g vercel

# Деплой
cd /root/.openclaw/workspace/dzen-auto
vercel --prod
```

### Вариант 2: Через GitHub (автоматический)

1. Создайте репозиторий на GitHub
2. Залейте код:

```bash
cd /root/.openclaw/workspace/dzen-auto
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/crypto-dzen.git
git push -u origin main
```

3. Подключите репозиторий к Vercel:
   - Зайдите на [vercel.com](https://vercel.com)
   - New Project → Import Git Repository
   - Выберите свой репозиторий
   - Deploy

### Вариант 3: Простой статический хостинг

Можно использовать любой статический хостинг:
- **Netlify** (netlify.com)
- **GitHub Pages** (pages.github.com)
- **Cloudflare Pages** (pages.cloudflare.com)

Все они поддерживают деплой из папки `public/`.

---

## ⚙️ Подключение к Яндекс.Дзен

### Шаг 1: Получить URL RSS

После деплоя ваш RSS будет доступен по адресу:
```
https://ваш-проект.vercel.app/rss.xml
```

### Шаг 2: Добавить в Дзен

1. Зайдите в **Яндекс.Дзен**
2. **Настройки канала** → **Источники**
3. **Добавить RSS**
4. Вставьте URL вашей ленты
5. Сохраните

Дзен будет автоматически проверять ленту и забирать новые статьи!

---

## 🤖 Полная автоматизация

### Текущий workflow (полуавтомат):

**12:00 MSK** — Генерация Telegram дайджеста (уже настроено ✅)

**Вручную:**
```bash
cd /root/.openclaw/workspace/dzen-auto

# Конвертировать дайджест
python3 convert_digest_to_article.py

# Сгенерировать RSS
python3 generate_rss_simple.py

# Закоммитить и запушить (если используете GitHub)
git add .
git commit -m "New digest $(date +%Y-%m-%d)"
git push

# Vercel автоматически деплоит → Дзен забирает статью
```

### Полная автоматизация (через cron):

Создайте cron задачу в OpenClaw:

```json
{
  "name": "Публикация в Дзен",
  "schedule": {"kind": "cron", "expr": "30 9 * * *"},
  "payload": {
    "kind": "agentTurn",
    "message": "Выполни:\n1. cd /root/.openclaw/workspace/dzen-auto\n2. python3 convert_digest_to_article.py\n3. python3 generate_rss_simple.py\n4. git add . && git commit -m 'Auto update' && git push\n5. Сообщи о результате"
  }
}
```

---

## 📁 Структура проекта

```
dzen-auto/
├── public/
│   ├── index.html          # Лендинг
│   ├── rss.xml             # RSS лента
│   └── articles/           # Статьи
│       ├── *.json          # JSON данные
│       └── *.md            # Markdown для Next.js
│
├── convert_digest_to_article.py   # Конвертер
├── generate_rss_simple.py         # RSS генератор
├── package.json                   # npm конфиг (опционально)
└── README.md                      # Эта инструкция
```

---

## 🎨 Улучшения (опционально)

### 1. Генерация изображений

Автоматически создавать обложки для статей:

```python
# Использовать:
# - Unsplash API (бесплатные фото)
# - Pillow (генерация с текстом)
# - Stable Diffusion (AI генерация)
```

### 2. AI улучшение контента

Прогонять дайджесты через GPT для:
- Улучшения стиля
- Добавления контекста
- SEO оптимизации

### 3. Парсинг дополнительных источников

Добавить контент из:
- CoinDesk, CoinTelegraph (новости)
- Twitter (топ твиты)
- Reddit crypto (обсуждения)

### 4. Аналитика

Отслеживать:
- Сколько статей импортировал Дзен
- Просмотры в Дзен
- Популярные темы

---

## 🔧 Настройка

### Изменить URL сайта

В `generate_rss_simple.py` замените:

```python
site_url="https://crypto-dzen.vercel.app"  # ← Ваш реальный URL после деплоя
```

### Изменить метаданные канала

В `generate_rss_simple.py` настройте:
- **title** — название канала
- **description** — описание
- **language** — язык (ru/en)

---

## 📊 Пример использования

### Создать статью из дайджеста

```bash
python3 convert_digest_to_article.py
```

**Результат:**
- `public/articles/crypto-digest-2026-02-08.json`
- `public/articles/crypto-digest-2026-02-08.md`

### Сгенерировать RSS

```bash
python3 generate_rss_simple.py
```

**Результат:**
- `public/rss.xml` — готовая RSS лента

### Проверить RSS

```bash
cat public/rss.xml
# или открыть в браузере
```

---

## ❓ FAQ

### Как часто Дзен проверяет RSS?

Обычно **каждые 30-60 минут**. Новые статьи появляются в течение 1-2 часов.

### Можно ли публиковать в несколько каналов Дзен?

Да! Один RSS можно подключить к нескольким каналам.

### Нужно ли создавать Next.js сайт?

Нет! Простой статический HTML (как сейчас) достаточен. Next.js нужен только для динамических фич.

### Как добавить вторую статью?

Просто запустите `convert_digest_to_article.py` снова — новая статья добавится в RSS.

### Можно ли редактировать статьи после публикации?

Да! Отредактируйте файл в `public/articles/`, перегенерируйте RSS, и Дзен обновит статью.

---

## 📞 Поддержка

Если нужна помощь с:
- Деплоем на Vercel
- Настройкой автоматизации
- Парсингом других источников
- AI улучшением контента

Просто спросите! 🚀

---

**Статус:** ✅ Готово к деплою  
**Текущие статьи:** 1 (crypto-digest-2026-02-08)  
**RSS:** Сгенерирован и готов
